# Project instructions

- Never force-push or rewrite history on `main` without explicit confirmation from the user in chat. Prefer a normal merge/push; if a force-push genuinely seems necessary, stop and ask first, even if the change being overwritten looks trivial.
